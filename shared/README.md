# Shared Directory

This directory contains utilities and definitions shared between the client and server (if applicable, or just logically shared concepts).

## Subdirectories

- `constants/`: Shared constants (API endpoints, status codes).
- `types/`: Type definitions.
- `validators/`: Shared validation logic.

# Assets Directory

This directory contains static assets for the client application.

## Subdirectories

- `images/`: Stores image files (PNG, JPG, SVG, etc.).
- `icons/`: Stores icon files.
- `fonts/`: Stores custom font files.

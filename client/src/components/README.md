# Components Directory

This directory contains reusable UI components.

## Subdirectories

- `common/`: Generic, atomic components (Buttons, Inputs, etc.) used across the app.
- `layout/`: Layout-specific components (Navbar, Sidebar, Footer, etc.).

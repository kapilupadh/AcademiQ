# Notifications Module

Handles application-wide notifications (Email, SMS, In-app).

## API Endpoints

- `POST /api/notifications/send`
- `GET /api/notifications/user/:id`

## Models

- `Notification`
- `NotificationTemplate`
